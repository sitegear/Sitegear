<?php
/**
 * @var \Sitegear\Core\View\View $view
 * @var \Sitegear\Ext\Module\Locations\Model\Region $region
 * @var \Sitegear\Ext\Module\Locations\Model\Region $subRegion
 * @var \Sitegear\Ext\Module\Locations\Model\Item $item
 */
$region = $view['region'];
$view->getStringsManager()->prepend('title', $view['title']);
$view->getStringsManager()->prepend('title', $region->getName());
echo sprintf('<h1>%s</h1>', $region->getName()), PHP_EOL;
echo $view->locations('editable')->item(sprintf('%s/%s', $view['region-path'], $region->getUrlPath()));
echo $view->locations()->item('partials/map');
echo $view->locations()->item('partials/items');
echo $view->locations()->item('partials/regions');
